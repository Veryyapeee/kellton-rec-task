export type Theme = {
  font: {
    primary: string;
    secondary: string;
    danger: string;
    contrast: string;
  };
  button: {
    backgroundPrimary: string;
    fontPrimary: string;
  };
  app: {
    backgroundPrimary: string;
    backgroundSecondary: string;
    contrast: string;
  };
};
