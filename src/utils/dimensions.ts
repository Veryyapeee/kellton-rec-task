import {Dimensions} from 'react-native';

const {width: screenWidth, height: screenHeight} = Dimensions.get('screen');

export const fullScreenWidth = screenWidth;
export const fullScreenHeight = screenHeight;
